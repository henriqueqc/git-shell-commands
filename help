echo "To manage the git repositories you can use the following commands:"
echo "    list             Lists all repositories"
echo "    new <name>       Creates a new repository named <name>"
echo "    remove <name>    Removes the repository named <name>"
echo
echo "When providing the <name> to the new and remove commands, there is no need to provide"
echo "the .git extension, it will be added for you."
